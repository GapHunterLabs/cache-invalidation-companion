<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.cacheinvalidationcompanion</id>
    <name>Cache-Invalidation Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on a <code>.save(...)</code> call inside a method
        whose class also has a hand-rolled <code>Map</code>-backed
        cache field (populated by a <code>computeIfAbsent</code>/manual
        <code>containsKey</code>+<code>put</code> "get-or-compute"
        pattern), when that method never touches the cache field --
        stale data served after a real update, a silent inconsistency
        between what's written and what's read.</p>
        <p>No Marketplace plugin found dedicated to hand-rolled cache
        invalidation gaps -- outside the <code>@Cacheable</code>
        ecosystem, which already has its own declarative invalidation
        mechanism.</p>
        <p>Stated honestly: v0.1 only a hand-rolled cache with
        <code>Map</code> as the backing structure inside a single class
        -- never covers <code>@Cacheable</code>/Caffeine/Redis as an
        external cache. "The same data source" is inferred
        heuristically (the mutator method never mentions the cache
        field's name at all) -- a real risk of false positives if that
        heuristic is loose, an honestly-acknowledged limit.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language="JAVA"
            groupName="Cache-Invalidation Companion"
            displayName="Data write without cache invalidation"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.cacheinvalidationcompanion.inspection.CacheInvalidationInspection"/>
        <notificationGroup id="Cache-Invalidation Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
